cache:
 - "%LOCALAPPDATA%\\Yarn"

install:
  - ps: yarn install

test_script:
  - node --version
  - yarn --version

build_script:
  - yarn run package

artifacts:
  - path: '_InstallPackages/*.zip'

notifications:
  - provider: GitHubPullRequest
